# ── Required for free demo ──────────────────────────────────────────────────
# Free token at huggingface.co → Settings → Access Tokens
HF_TOKEN=

# ── Optional — only needed if evaluator wants to test paid models ────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
